# Auto detect text files and perform LF normalization
* text=auto

# A betűk bináris fájlok: semmilyen sorvég-normalizálás nem érintheti őket,
# mert az szó szerint tönkretenné a woff2-t. A `text=auto` tartalom alapján
# amúgy is felismerné, de ezt nem bízzuk heurisztikára.
*.woff2 binary
